DB_HOST=127.0.0.1
DB_USER=root
DB_PASS=
DB_NAME=adobe_db

# 32 characters for AES-256
AES_KEY=0123456789abcdef0123456789abcdef
# 16 characters for IV
AES_IV=0123456789abcdef

# Cron schedule (e.g. every 15 minutes)
CRON_SCHEDULE="*/15 * * * *"

# Secret token for manual triggers
ADMIN_TOKEN=secret_admin_token

PORT=3000
